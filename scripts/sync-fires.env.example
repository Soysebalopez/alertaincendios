# Copy to scripts/sync-fires.env and fill in.
# Then: chmod 600 scripts/sync-fires.env
# The real .env file is in .gitignore so it never gets committed.

CRON_SECRET=PASTE_THE_VERCEL_CRON_SECRET_VALUE_HERE
# Optional: override the site URL if running against preview/staging
# SITE_URL=https://alertaincendios.vercel.app
